/* eslint-disable no-console */
// NOTE: This is a placeholder. Full deterministic demo-data seeding
// (20-30 incidents, 50-100 reports, 15-20 resources, 5-10 hospitals,
// intentional duplicates, resource failures) is built in Stage 16.

console.log('[seed] Not implemented yet - demo data seeding arrives in Stage 16.');
console.log('[seed] Schema is ready; run `npm run migrate` to (re)apply it.');
